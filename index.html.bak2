<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>BitLend — Lending Protocol on OP_NET</title>
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Syne:wght@400;600;700;800&display=swap" rel="stylesheet"/>
  <style>
    :root {
      --bg: #07080d;
      --surface: #0d0f1a;
      --surface2: #131627;
      --border: #1e2235;
      --accent: #f7931a;
      --accent2: #ff6b35;
      --green: #00d4a4;
      --red: #ff4d6d;
      --blue: #4d9fff;
      --text: #e8eaf0;
      --muted: #6b7290;
      --font-mono: 'Space Mono', monospace;
      --font-sans: 'Syne', sans-serif;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: var(--font-sans);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Background grid */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(247,147,26,0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(247,147,26,0.03) 1px, transparent 1px);
      background-size: 40px 40px;
      pointer-events: none;
      z-index: 0;
    }

    /* Glow blobs */
    .blob {
      position: fixed;
      border-radius: 50%;
      filter: blur(120px);
      opacity: 0.12;
      pointer-events: none;
      z-index: 0;
    }
    .blob-1 { width: 500px; height: 500px; background: var(--accent); top: -100px; right: -100px; }
    .blob-2 { width: 400px; height: 400px; background: var(--green); bottom: 0; left: -100px; }

    /* NAVBAR */
    nav {
      position: sticky;
      top: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 16px 32px;
      background: rgba(7,8,13,0.85);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 22px;
      font-weight: 800;
      letter-spacing: -0.5px;
    }
    .logo-icon {
      width: 36px; height: 36px;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      border-radius: 10px;
      display: flex; align-items: center; justify-content: center;
      font-size: 18px;
    }
    .logo span { color: var(--accent); }

    .nav-links {
      display: flex; gap: 32px; list-style: none;
    }
    .nav-links a {
      color: var(--muted); text-decoration: none;
      font-size: 14px; font-weight: 600; letter-spacing: 0.5px;
      transition: color 0.2s;
    }
    .nav-links a:hover, .nav-links a.active { color: var(--text); }

    .nav-right { display: flex; align-items: center; gap: 12px; }

    .network-badge {
      display: flex; align-items: center; gap: 6px;
      background: var(--surface2);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 6px 14px;
      font-size: 12px;
      font-family: var(--font-mono);
      color: var(--muted);
    }
    .network-dot {
      width: 7px; height: 7px; border-radius: 50%;
      background: var(--green);
      box-shadow: 0 0 6px var(--green);
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0%,100% { opacity: 1; } 50% { opacity: 0.4; }
    }

    .btn {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 10px 20px;
      border-radius: 10px;
      font-family: var(--font-sans);
      font-size: 14px;
      font-weight: 700;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
    }
    .btn-primary {
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      color: #000;
    }
    .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(247,147,26,0.4); }
    .btn-outline {
      background: transparent;
      color: var(--text);
      border: 1px solid var(--border);
    }
    .btn-outline:hover { border-color: var(--accent); color: var(--accent); }
    .btn-success {
      background: linear-gradient(135deg, var(--green), #00a884);
      color: #000;
    }
    .btn-success:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,212,164,0.4); }
    .btn-danger {
      background: linear-gradient(135deg, var(--red), #cc2244);
      color: #fff;
    }

    #connectBtn.connected {
      background: var(--surface2);
      border: 1px solid var(--green);
      color: var(--green);
    }

    /* MAIN LAYOUT */
    main {
      position: relative;
      z-index: 1;
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 24px 80px;
    }

    /* HERO STATS */
    .stats-bar {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
      margin-bottom: 40px;
    }
    .stat-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 20px 24px;
      transition: border-color 0.2s;
    }
    .stat-card:hover { border-color: rgba(247,147,26,0.3); }
    .stat-label {
      font-size: 11px;
      font-family: var(--font-mono);
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 8px;
    }
    .stat-value {
      font-size: 28px;
      font-weight: 800;
      background: linear-gradient(135deg, var(--text), var(--muted));
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
      color: transparent;
    }
    .stat-sub {
      font-size: 12px;
      color: var(--green);
      margin-top: 4px;
      font-weight: 600;
    }

    /* TABS */
    .tabs {
      display: flex;
      gap: 4px;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 6px;
      margin-bottom: 28px;
      width: fit-content;
    }
    .tab {
      padding: 10px 24px;
      border-radius: 10px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 700;
      color: var(--muted);
      transition: all 0.2s;
      border: none;
      background: transparent;
      font-family: var(--font-sans);
    }
    .tab.active {
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      color: #000;
    }
    .tab:hover:not(.active) { color: var(--text); }

    /* CONTENT PANELS */
    .panel { display: none; }
    .panel.active { display: block; }

    /* MARKET TABLE */
    .section-title {
      font-size: 20px;
      font-weight: 800;
      margin-bottom: 4px;
    }
    .section-sub {
      font-size: 13px;
      color: var(--muted);
      margin-bottom: 24px;
    }

    .market-table {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 20px;
      overflow: hidden;
    }
    .table-header {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr 1fr 1fr 1.5fr;
      padding: 14px 24px;
      font-size: 11px;
      font-family: var(--font-mono);
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      border-bottom: 1px solid var(--border);
    }
    .market-row {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr 1fr 1fr 1.5fr;
      padding: 20px 24px;
      align-items: center;
      border-bottom: 1px solid var(--border);
      transition: background 0.15s;
    }
    .market-row:last-child { border-bottom: none; }
    .market-row:hover { background: var(--surface2); }

    .asset-info {
      display: flex; align-items: center; gap: 14px;
    }
    .asset-icon {
      width: 42px; height: 42px;
      border-radius: 12px;
      display: flex; align-items: center; justify-content: center;
      font-size: 22px;
      font-weight: 800;
    }
    .btc-icon { background: linear-gradient(135deg, rgba(247,147,26,0.2), rgba(247,147,26,0.1)); color: var(--accent); }
    .usds-icon { background: linear-gradient(135deg, rgba(0,212,164,0.2), rgba(0,212,164,0.1)); color: var(--green); }
    .wbtc-icon { background: linear-gradient(135deg, rgba(77,159,255,0.2), rgba(77,159,255,0.1)); color: var(--blue); }

    .asset-name { font-size: 15px; font-weight: 700; }
    .asset-full { font-size: 12px; color: var(--muted); }

    .rate-up { color: var(--green); font-weight: 700; font-size: 15px; }
    .rate-neutral { color: var(--blue); font-weight: 700; font-size: 15px; }

    .utilization-bar {
      width: 100%;
      height: 6px;
      background: var(--border);
      border-radius: 3px;
      overflow: hidden;
      margin-top: 4px;
    }
    .utilization-fill {
      height: 100%;
      border-radius: 3px;
      background: linear-gradient(90deg, var(--accent), var(--accent2));
    }
    .util-text { font-size: 12px; color: var(--muted); }

    .row-actions { display: flex; gap: 8px; justify-content: flex-end; }
    .btn-sm {
      padding: 7px 16px;
      font-size: 12px;
      font-weight: 700;
      border-radius: 8px;
      border: none;
      cursor: pointer;
      font-family: var(--font-sans);
      transition: all 0.2s;
    }
    .btn-sm-green { background: rgba(0,212,164,0.15); color: var(--green); border: 1px solid rgba(0,212,164,0.3); }
    .btn-sm-green:hover { background: var(--green); color: #000; }
    .btn-sm-orange { background: rgba(247,147,26,0.15); color: var(--accent); border: 1px solid rgba(247,147,26,0.3); }
    .btn-sm-orange:hover { background: var(--accent); color: #000; }

    /* POSITION CARDS */
    .position-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }
    .position-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 28px;
    }
    .position-card h3 {
      font-size: 13px;
      font-family: var(--font-mono);
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 20px;
    }
    .position-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 14px 0;
      border-bottom: 1px solid var(--border);
    }
    .position-item:last-child { border-bottom: none; }
    .pos-left { display: flex; align-items: center; gap: 10px; }
    .pos-name { font-size: 15px; font-weight: 700; }
    .pos-apy { font-size: 12px; color: var(--green); }
    .pos-amount { font-size: 16px; font-weight: 800; }
    .pos-value { font-size: 12px; color: var(--muted); text-align: right; }

    /* HEALTH FACTOR */
    .health-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 28px;
      margin-top: 20px;
    }
    .health-bar-wrap {
      background: var(--border);
      border-radius: 8px;
      height: 12px;
      overflow: hidden;
      margin: 16px 0 8px;
    }
    .health-bar-fill {
      height: 100%;
      border-radius: 8px;
      background: linear-gradient(90deg, var(--red), var(--accent), var(--green));
      width: 72%;
      transition: width 0.8s ease;
    }
    .health-labels {
      display: flex;
      justify-content: space-between;
      font-size: 11px;
      color: var(--muted);
      font-family: var(--font-mono);
    }
    .health-value {
      font-size: 40px;
      font-weight: 800;
      color: var(--green);
    }

    /* MODAL */
    .modal-overlay {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(7,8,13,0.8);
      backdrop-filter: blur(10px);
      z-index: 200;
      align-items: center;
      justify-content: center;
    }
    .modal-overlay.open { display: flex; }

    .modal {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 24px;
      padding: 32px;
      width: 100%;
      max-width: 420px;
      animation: slideUp 0.3s ease;
    }
    @keyframes slideUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .modal-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 24px;
    }
    .modal-title { font-size: 20px; font-weight: 800; }
    .modal-close {
      width: 32px; height: 32px;
      background: var(--surface2);
      border: 1px solid var(--border);
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      color: var(--muted);
      display: flex; align-items: center; justify-content: center;
    }
    .modal-close:hover { color: var(--text); }

    .modal-asset {
      display: flex; align-items: center; gap: 14px;
      background: var(--surface2);
      border-radius: 14px;
      padding: 16px;
      margin-bottom: 20px;
    }
    .modal-asset-name { font-size: 18px; font-weight: 800; }
    .modal-asset-sub { font-size: 12px; color: var(--muted); }

    .input-group { margin-bottom: 16px; }
    .input-label {
      font-size: 12px;
      font-family: var(--font-mono);
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
      display: flex;
      justify-content: space-between;
    }
    .input-wrap {
      display: flex;
      align-items: center;
      background: var(--surface2);
      border: 1px solid var(--border);
      border-radius: 12px;
      overflow: hidden;
      transition: border-color 0.2s;
    }
    .input-wrap:focus-within { border-color: var(--accent); }
    .input-wrap input {
      flex: 1;
      background: transparent;
      border: none;
      color: var(--text);
      font-size: 18px;
      font-family: var(--font-mono);
      padding: 14px 16px;
      outline: none;
    }
    .input-max {
      padding: 8px 14px;
      font-size: 11px;
      font-weight: 700;
      color: var(--accent);
      cursor: pointer;
      font-family: var(--font-sans);
      border-left: 1px solid var(--border);
    }
    .input-max:hover { color: var(--accent2); }

    .modal-info-row {
      display: flex; justify-content: space-between;
      font-size: 13px;
      padding: 8px 0;
      border-bottom: 1px solid var(--border);
    }
    .modal-info-row:last-of-type { border-bottom: none; }
    .info-label { color: var(--muted); }
    .info-value { font-weight: 700; }
    .info-value.green { color: var(--green); }
    .info-value.orange { color: var(--accent); }

    .modal-actions {
      display: flex; gap: 10px; margin-top: 24px;
    }
    .modal-actions .btn { flex: 1; justify-content: center; }

    /* AGENT CHAT */
    .agent-panel {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 20px;
      overflow: hidden;
      height: 560px;
      display: flex;
      flex-direction: column;
    }
    .agent-header {
      padding: 20px 24px;
      border-bottom: 1px solid var(--border);
      display: flex;
      align-items: center;
      gap: 14px;
      background: var(--surface2);
    }
    .agent-avatar {
      width: 42px; height: 42px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      display: flex; align-items: center; justify-content: center;
      font-size: 20px;
    }
    .agent-name { font-size: 16px; font-weight: 800; }
    .agent-status { font-size: 12px; color: var(--green); }
    .agent-badge {
      margin-left: auto;
      background: rgba(247,147,26,0.15);
      color: var(--accent);
      border: 1px solid rgba(247,147,26,0.3);
      padding: 4px 10px;
      border-radius: 20px;
      font-size: 11px;
      font-family: var(--font-mono);
    }

    .chat-messages {
      flex: 1;
      overflow-y: auto;
      padding: 20px;
      display: flex;
      flex-direction: column;
      gap: 16px;
      scrollbar-width: thin;
      scrollbar-color: var(--border) transparent;
    }

    .msg {
      display: flex;
      gap: 12px;
      animation: fadeIn 0.3s ease;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .msg.user { flex-direction: row-reverse; }
    .msg-avatar {
      width: 32px; height: 32px; border-radius: 50%;
      flex-shrink: 0;
      display: flex; align-items: center; justify-content: center;
      font-size: 14px;
    }
    .msg-avatar.bot { background: linear-gradient(135deg, var(--accent), var(--accent2)); }
    .msg-avatar.user-av { background: var(--surface2); border: 1px solid var(--border); }
    .msg-bubble {
      max-width: 75%;
      padding: 12px 16px;
      border-radius: 16px;
      font-size: 14px;
      line-height: 1.6;
    }
    .msg.bot .msg-bubble {
      background: var(--surface2);
      border: 1px solid var(--border);
      border-top-left-radius: 4px;
    }
    .msg.user .msg-bubble {
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      color: #000;
      border-top-right-radius: 4px;
    }
    .msg-time {
      font-size: 10px;
      color: var(--muted);
      margin-top: 4px;
      font-family: var(--font-mono);
    }
    .msg.user .msg-time { text-align: right; }

    .typing-indicator {
      display: flex; gap: 4px; padding: 4px 0;
    }
    .typing-dot {
      width: 8px; height: 8px; border-radius: 50%;
      background: var(--muted);
      animation: typingBounce 1.2s infinite;
    }
    .typing-dot:nth-child(2) { animation-delay: 0.2s; }
    .typing-dot:nth-child(3) { animation-delay: 0.4s; }
    @keyframes typingBounce {
      0%,60%,100% { transform: translateY(0); }
      30% { transform: translateY(-8px); }
    }

    .quick-prompts {
      padding: 12px 20px;
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      border-top: 1px solid var(--border);
      background: var(--surface2);
    }
    .quick-btn {
      padding: 6px 12px;
      font-size: 12px;
      font-weight: 600;
      background: transparent;
      border: 1px solid var(--border);
      color: var(--muted);
      border-radius: 20px;
      cursor: pointer;
      font-family: var(--font-sans);
      transition: all 0.2s;
      white-space: nowrap;
    }
    .quick-btn:hover { border-color: var(--accent); color: var(--accent); }

    .chat-input-area {
      padding: 16px 20px;
      border-top: 1px solid var(--border);
      display: flex;
      gap: 12px;
    }
    .chat-input {
      flex: 1;
      background: var(--surface2);
      border: 1px solid var(--border);
      border-radius: 12px;
      color: var(--text);
      font-size: 14px;
      font-family: var(--font-sans);
      padding: 12px 16px;
      outline: none;
      transition: border-color 0.2s;
    }
    .chat-input:focus { border-color: var(--accent); }
    .chat-send {
      width: 44px; height: 44px;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      border: none;
      border-radius: 12px;
      cursor: pointer;
      display: flex; align-items: center; justify-content: center;
      font-size: 18px;
      transition: all 0.2s;
    }
    .chat-send:hover { transform: scale(1.05); box-shadow: 0 4px 16px rgba(247,147,26,0.4); }

    /* WALLET PANEL */
    .wallet-overlay {
      display: none;
      position: fixed;
      top: 70px; right: 24px;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 24px;
      width: 320px;
      z-index: 150;
      box-shadow: 0 20px 60px rgba(0,0,0,0.6);
      animation: slideDown 0.2s ease;
    }
    @keyframes slideDown {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .wallet-overlay.open { display: block; }

    .wallet-address {
      font-family: var(--font-mono);
      font-size: 12px;
      color: var(--muted);
      background: var(--surface2);
      border-radius: 10px;
      padding: 10px 14px;
      margin-bottom: 16px;
      word-break: break-all;
    }
    .wallet-balance {
      font-size: 32px;
      font-weight: 800;
      margin-bottom: 4px;
    }
    .wallet-sub { font-size: 13px; color: var(--muted); margin-bottom: 20px; }

    .wallet-actions {
      display: grid; grid-template-columns: 1fr 1fr; gap: 10px;
    }

    /* TOAST */
    .toast {
      position: fixed;
      bottom: 32px;
      right: 32px;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 16px 20px;
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 14px;
      z-index: 300;
      transform: translateY(100px);
      opacity: 0;
      transition: all 0.3s;
      max-width: 360px;
    }
    .toast.show { transform: translateY(0); opacity: 1; }
    .toast-icon { font-size: 20px; }
    .toast-title { font-weight: 700; }
    .toast-msg { font-size: 12px; color: var(--muted); margin-top: 2px; }

    /* MCP INDICATOR */
    .mcp-badge {
      display: flex;
      align-items: center;
      gap: 6px;
      background: rgba(77,159,255,0.1);
      border: 1px solid rgba(77,159,255,0.25);
      border-radius: 20px;
      padding: 5px 12px;
      font-size: 11px;
      color: var(--blue);
      font-family: var(--font-mono);
    }
    .mcp-dot {
      width: 6px; height: 6px;
      border-radius: 50%;
      background: var(--blue);
      animation: pulse 2s infinite;
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
      .stats-bar { grid-template-columns: repeat(2, 1fr); }
      .table-header, .market-row { grid-template-columns: 2fr 1fr 1fr; }
      .market-row > *:nth-child(n+4) { display: none; }
      .position-grid { grid-template-columns: 1fr; }
      nav { padding: 12px 16px; }
      .nav-links { display: none; }
    }
  </style>
</head>
<body>

<div class="blob blob-1"></div>
<div class="blob blob-2"></div>

<!-- NAVBAR -->
<nav>
  <div class="logo">
    <div class="logo-icon">₿</div>
    Bit<span>Lend</span>
  </div>
  <ul class="nav-links">
    <li><a href="#" class="active" onclick="showTab('markets')">Markets</a></li>
    <li><a href="#" onclick="showTab('positions')">My Positions</a></li>
    <li><a href="#" onclick="showTab('agent')">AI Agent</a></li>
  </ul>
  <div class="nav-right">
    <div class="mcp-badge">
      <div class="mcp-dot"></div>
      MCP: ai.opnet.org
    </div>
    <div class="network-badge">
      <div class="network-dot"></div>
      <span id="networkName">OP_NET Testnet</span>
    </div>
    <button class="btn btn-primary" id="connectBtn" onclick="toggleWallet()">
      🔐 Connect Wallet
    </button>
  </div>
</nav>

<!-- WALLET DROPDOWN -->
<div class="wallet-overlay" id="walletOverlay">
  <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:16px;">
    <span style="font-weight:800; font-size:16px;">OP_NET Wallet</span>
    <button class="modal-close" onclick="closeWallet()">✕</button>
  </div>
  <div class="wallet-address" id="walletAddr">Not connected</div>
  <div class="wallet-balance" id="walletBal">0.0000 <span style="font-size:16px;color:var(--muted)">tBTC</span></div>
  <div class="wallet-sub" id="walletUSD">≈ $0.00 USD</div>
  <div class="wallet-actions">
    <button class="btn btn-sm btn-sm-orange" onclick="requestFaucet()">🚰 Faucet</button>
    <button class="btn btn-sm btn-sm-green" onclick="disconnectWallet()">Disconnect</button>
  </div>
</div>

<!-- MAIN -->
<main>
  <!-- STATS BAR -->
  <div class="stats-bar">
    <div class="stat-card">
      <div class="stat-label">Total Value Locked</div>
      <div class="stat-value" id="tvl">$4.21M</div>
      <div class="stat-sub">↑ 12.4% this week</div>
    </div>
    <div class="stat-card">
      <div class="stat-label">Total Borrowed</div>
      <div class="stat-value" id="borrowed">$1.87M</div>
      <div class="stat-sub">44.4% utilization</div>
    </div>
    <div class="stat-card">
      <div class="stat-label">Available to Borrow</div>
      <div class="stat-value">$2.34M</div>
      <div class="stat-sub">Across 3 markets</div>
    </div>
    <div class="stat-card">
      <div class="stat-label">Protocol Revenue</div>
      <div class="stat-value">$18.2K</div>
      <div class="stat-sub">Last 30 days</div>
    </div>
  </div>

  <!-- TABS -->
  <div class="tabs">
    <button class="tab active" id="tab-markets" onclick="showTab('markets')">Markets</button>
    <button class="tab" id="tab-positions" onclick="showTab('positions')">My Positions</button>
    <button class="tab" id="tab-agent" onclick="showTab('agent')">⚡ AI Agent</button>
  </div>

  <!-- MARKETS PANEL -->
  <div class="panel active" id="panel-markets">
    <div class="section-title">Lending Markets</div>
    <div class="section-sub">Supply assets to earn yield or borrow against your collateral on OP_NET Bitcoin L1</div>

    <div class="market-table">
      <div class="table-header">
        <div>Asset</div>
        <div>Total Supply</div>
        <div>Supply APY</div>
        <div>Total Borrowed</div>
        <div>Borrow APY</div>
        <div style="text-align:right">Actions</div>
      </div>

      <!-- tBTC Row -->
      <div class="market-row">
        <div class="asset-info">
          <div class="asset-icon btc-icon">₿</div>
          <div>
            <div class="asset-name">tBTC</div>
            <div class="asset-full">Testnet Bitcoin</div>
          </div>
        </div>
        <div>
          <div style="font-weight:700">124.8 tBTC</div>
          <div class="util-text">$4.21M</div>
        </div>
        <div class="rate-up">2.84%</div>
        <div>
          <div style="font-weight:700">54.2 tBTC</div>
          <div class="utilization-bar"><div class="utilization-fill" style="width:43%"></div></div>
          <div class="util-text">43% utilized</div>
        </div>
        <div class="rate-neutral">5.61%</div>
        <div class="row-actions">
          <button class="btn-sm btn-sm-green" onclick="openModal('supply','tBTC','2.84')">Supply</button>
          <button class="btn-sm btn-sm-orange" onclick="openModal('borrow','tBTC','5.61')">Borrow</button>
        </div>
      </div>

      <!-- USDs Row -->
      <div class="market-row">
        <div class="asset-info">
          <div class="asset-icon usds-icon">$</div>
          <div>
            <div class="asset-name">USDs</div>
            <div class="asset-full">OP_NET Stablecoin</div>
          </div>
        </div>
        <div>
          <div style="font-weight:700">2.1M USDs</div>
          <div class="util-text">$2.1M</div>
        </div>
        <div class="rate-up">4.21%</div>
        <div>
          <div style="font-weight:700">1.4M USDs</div>
          <div class="utilization-bar"><div class="utilization-fill" style="width:67%"></div></div>
          <div class="util-text">67% utilized</div>
        </div>
        <div class="rate-neutral">7.85%</div>
        <div class="row-actions">
          <button class="btn-sm btn-sm-green" onclick="openModal('supply','USDs','4.21')">Supply</button>
          <button class="btn-sm btn-sm-orange" onclick="openModal('borrow','USDs','7.85')">Borrow</button>
        </div>
      </div>

      <!-- WBTC Row -->
      <div class="market-row">
        <div class="asset-info">
          <div class="asset-icon wbtc-icon">Ƀ</div>
          <div>
            <div class="asset-name">WBTC</div>
            <div class="asset-full">Wrapped Bitcoin</div>
          </div>
        </div>
        <div>
          <div style="font-weight:700">18.3 WBTC</div>
          <div class="util-text">$618K</div>
        </div>
        <div class="rate-up">3.15%</div>
        <div>
          <div style="font-weight:700">7.1 WBTC</div>
          <div class="utilization-bar"><div class="utilization-fill" style="width:39%"></div></div>
          <div class="util-text">39% utilized</div>
        </div>
        <div class="rate-neutral">6.02%</div>
        <div class="row-actions">
          <button class="btn-sm btn-sm-green" onclick="openModal('supply','WBTC','3.15')">Supply</button>
          <button class="btn-sm btn-sm-orange" onclick="openModal('borrow','WBTC','6.02')">Borrow</button>
        </div>
      </div>
    </div>
  </div>

  <!-- POSITIONS PANEL -->
  <div class="panel" id="panel-positions">
    <div class="section-title">My Positions</div>
    <div class="section-sub">Your active lending and borrowing positions on OP_NET</div>

    <div id="noPositions" style="text-align:center; padding:60px 20px; color:var(--muted);">
      <div style="font-size:48px; margin-bottom:16px;">📊</div>
      <div style="font-size:18px; font-weight:700; margin-bottom:8px;">No Active Positions</div>
      <div style="font-size:14px; margin-bottom:24px;">Connect your OP_NET wallet and supply or borrow to get started</div>
      <button class="btn btn-primary" onclick="showTab('markets')">Go to Markets</button>
    </div>

    <div id="positionsContent" style="display:none">
      <div class="position-grid">
        <div class="position-card">
          <h3>💰 Supplied Assets</h3>
          <div class="position-item">
            <div class="pos-left">
              <div class="asset-icon btc-icon" style="width:36px;height:36px;font-size:18px;">₿</div>
              <div>
                <div class="pos-name">tBTC</div>
                <div class="pos-apy">APY: 2.84%</div>
              </div>
            </div>
            <div>
              <div class="pos-amount">0.25 tBTC</div>
              <div class="pos-value">≈ $8,425</div>
            </div>
          </div>
          <div class="position-item">
            <div class="pos-left">
              <div class="asset-icon usds-icon" style="width:36px;height:36px;font-size:18px;">$</div>
              <div>
                <div class="pos-name">USDs</div>
                <div class="pos-apy">APY: 4.21%</div>
              </div>
            </div>
            <div>
              <div class="pos-amount">5,000 USDs</div>
              <div class="pos-value">≈ $5,000</div>
            </div>
          </div>
        </div>
        <div class="position-card">
          <h3>📤 Borrowed Assets</h3>
          <div class="position-item">
            <div class="pos-left">
              <div class="asset-icon usds-icon" style="width:36px;height:36px;font-size:18px;">$</div>
              <div>
                <div class="pos-name">USDs</div>
                <div class="pos-apy" style="color:var(--accent)">APY: 7.85%</div>
              </div>
            </div>
            <div>
              <div class="pos-amount">2,000 USDs</div>
              <div class="pos-value">≈ $2,000</div>
            </div>
          </div>
        </div>
      </div>
      <div class="health-card">
        <div style="display:flex; justify-content:space-between; align-items:flex-start;">
          <div>
            <div style="font-size:13px; font-family:var(--font-mono); color:var(--muted); text-transform:uppercase; letter-spacing:1px; margin-bottom:8px;">Health Factor</div>
            <div class="health-value">2.14</div>
            <div style="font-size:13px; color:var(--muted); margin-top:4px;">Position is safe — liquidation below 1.0</div>
          </div>
          <div style="text-align:right;">
            <div style="font-size:13px; color:var(--muted);">Net APY</div>
            <div style="font-size:24px; font-weight:800; color:var(--green);">+1.82%</div>
          </div>
        </div>
        <div class="health-bar-wrap"><div class="health-bar-fill"></div></div>
        <div class="health-labels"><span>Liquidation</span><span>Risky</span><span>Safe</span><span>Very Safe</span></div>
      </div>
    </div>
  </div>

  <!-- AI AGENT PANEL -->
  <div class="panel" id="panel-agent">
    <div class="section-title">AI Agent — Powered by OP_NET MCP</div>
    <div class="section-sub">Intelligent DeFi assistant connected to <code style="color:var(--accent)">ai.opnet.org/mcp</code> — ask anything about markets, positions, or execute transactions</div>

    <div class="agent-panel">
      <div class="agent-header">
        <div class="agent-avatar">🤖</div>
        <div>
          <div class="agent-name">BitLend AI Agent</div>
          <div class="agent-status">● Connected to OP_NET MCP</div>
        </div>
        <div class="agent-badge">MCP: ai.opnet.org</div>
      </div>

      <div class="chat-messages" id="chatMessages">
        <div class="msg bot">
          <div class="msg-avatar bot">🤖</div>
          <div>
            <div class="msg-bubble">
              Hello! I'm BitLend's AI agent, connected to <strong>OP_NET MCP</strong> at <code>ai.opnet.org/mcp</code>. I can help you:<br><br>
              • 📊 Analyze market conditions and APY rates<br>
              • 💰 Manage your supply/borrow positions<br>
              • ⚡ Execute transactions on OP_NET testnet<br>
              • 🔍 Query your wallet balance and history<br><br>
              Connect your OP_NET wallet to get started!
            </div>
            <div class="msg-time">Today, just now</div>
          </div>
        </div>
      </div>

      <div class="quick-prompts">
        <button class="quick-btn" onclick="sendQuick('What are the current APY rates?')">Current APY rates</button>
        <button class="quick-btn" onclick="sendQuick('Show my wallet balance')">My balance</button>
        <button class="quick-btn" onclick="sendQuick('What is the health factor for my position?')">Health factor</button>
        <button class="quick-btn" onclick="sendQuick('Best yield strategy for tBTC?')">Best strategy</button>
        <button class="quick-btn" onclick="sendQuick('Supply 0.1 tBTC to earn yield')">Supply tBTC</button>
      </div>

      <div class="chat-input-area">
        <input class="chat-input" id="chatInput" type="text"
          placeholder="Ask anything about lending, rates, or your positions..."
          onkeydown="if(event.key==='Enter') sendChat()"
        />
        <button class="chat-send" onclick="sendChat()">➤</button>
      </div>
    </div>
  </div>
</main>

<!-- SUPPLY/BORROW MODAL -->
<div class="modal-overlay" id="modalOverlay" onclick="closeModalIfBg(event)">
  <div class="modal">
    <div class="modal-header">
      <div class="modal-title" id="modalTitle">Supply Asset</div>
      <button class="modal-close" onclick="closeModal()">✕</button>
    </div>

    <div class="modal-asset">
      <div class="asset-icon btc-icon" id="modalIcon" style="width:42px;height:42px;">₿</div>
      <div>
        <div class="modal-asset-name" id="modalAssetName">tBTC</div>
        <div class="modal-asset-sub" id="modalAssetSub">Testnet Bitcoin • OP_NET</div>
      </div>
    </div>

    <div class="input-group">
      <div class="input-label">
        <span>Amount</span>
        <span>Balance: <span id="modalBalance">0.0000</span></span>
      </div>
      <div class="input-wrap">
        <input type="number" id="amountInput" placeholder="0.00" step="0.001" min="0"/>
        <div class="input-max" onclick="setMax()">MAX</div>
      </div>
    </div>

    <div class="modal-info-row">
      <span class="info-label">APY</span>
      <span class="info-value green" id="modalApy">2.84%</span>
    </div>
    <div class="modal-info-row">
      <span class="info-label">Network Fee</span>
      <span class="info-value">~1,200 sats</span>
    </div>
    <div class="modal-info-row">
      <span class="info-label">Protocol</span>
      <span class="info-value orange">OP_NET Testnet</span>
    </div>
    <div class="modal-info-row">
      <span class="info-label">Health Factor</span>
      <span class="info-value green">2.14 → 2.31</span>
    </div>

    <div class="modal-actions">
      <button class="btn btn-outline" onclick="closeModal()">Cancel</button>
      <button class="btn btn-primary" id="modalConfirmBtn" onclick="confirmTx()">
        Confirm Supply
      </button>
    </div>
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="toast">
  <div class="toast-icon" id="toastIcon">✅</div>
  <div>
    <div class="toast-title" id="toastTitle">Success</div>
    <div class="toast-msg" id="toastMsg">Transaction confirmed</div>
  </div>
</div>

<script>
// ============================================================
// CONFIG — Update contract address after deployment
// ============================================================
// Alamat smart contract lending di OP_NET Testnet
// Ganti dengan alamat kontrak kamu setelah deploy
// Panduan deploy: lihat README.md bagian "Smart Contract"
const LENDING_CONTRACT = 'opt1sqr9wzntcpn26x3fg380uszv8ncczjgylds5r5244';

// RPC endpoint OP_NET Testnet
const OPNET_RPC = 'https://testnet.opnet.org';

// Explorer untuk cek transaksi
const EXPLORER_URL = 'https://explorer.opnet.org/tx/';

// ============================================================
// STATE
// ============================================================
let walletConnected = false;
let walletAddress = null;
let walletBalance = 0;
let currentModal = null;
let isAgentThinking = false;

// ============================================================
// WALLET MANAGEMENT
// ============================================================
async function toggleWallet() {
  if (walletConnected) {
    document.getElementById('walletOverlay').classList.toggle('open');
  } else {
    await connectWallet();
  }
}

function closeWallet() {
  document.getElementById('walletOverlay').classList.remove('open');
}

async function connectWallet() {
  // ── Cek OPWallet extension ──
  const provider = window.opnet || window.bitcoin || window.unisat;

  if (!provider) {
    showToast('⚠️', 'OPWallet Not Found', 'Please install OPWallet Chrome Extension first!');
    setTimeout(function() {
      window.open('https://chromewebstore.google.com/detail/opwallet/pmbjpcmaaladnfpacpmhmnfmpklgbdjb', '_blank');
    }, 1200);
    return;
  }

  try {
    // ── Minta izin akses akun ──
    let accounts = [];
    if (typeof provider.requestAccounts === 'function') {
      accounts = await provider.requestAccounts();
    } else if (typeof provider.getAccounts === 'function') {
      accounts = await provider.getAccounts();
    }

    if (!accounts || accounts.length === 0) {
      showToast('⚠️', 'No Accounts', 'No accounts found. Please unlock OPWallet.');
      return;
    }

    walletAddress = accounts[0];

    // ── Ambil saldo tBTC ──
    try {
      let bal = 0;
      if (typeof provider.getBalance === 'function') {
        const result = await provider.getBalance();
        if (typeof result === 'object' && result !== null) {
          bal = (parseInt(result.confirmed || result.total || 0)) / 1e8;
        } else {
          bal = parseInt(result || 0) / 1e8;
        }
      }
      walletBalance = bal;
    } catch (balErr) {
      console.warn('Balance fetch error:', balErr);
      walletBalance = 0;
    }

    // ── Cek network — harus Testnet ──
    try {
      if (typeof provider.getNetwork === 'function') {
        const network = await provider.getNetwork();
        const networkName = (network || '').toString().toLowerCase();
        if (networkName.includes('main') && !networkName.includes('test')) {
          showToast('⚠️', 'Wrong Network', 'Please switch OPWallet to Testnet 3!');
          return;
        }
      }
    } catch (netErr) {
      console.warn('Network check error:', netErr);
    }

    // ── Event listener perubahan akun & network ──
    if (provider.on) {
      provider.on('accountsChanged', function(newAccounts) {
        if (!newAccounts || newAccounts.length === 0) {
          disconnectWallet();
        } else {
          walletAddress = newAccounts[0];
          document.getElementById('walletAddr').textContent = walletAddress;
          document.getElementById('connectBtn').textContent =
            walletAddress.slice(0, 8) + '...' + walletAddress.slice(-4);
          showToast('🔄', 'Account Changed', walletAddress.slice(0,12) + '...');
        }
      });
      provider.on('networkChanged', function(newNetwork) {
        showToast('🌐', 'Network Changed', 'Switched to: ' + newNetwork);
        document.getElementById('networkName').textContent = newNetwork;
      });
    }

    finishConnect(walletAddress, walletBalance);

  } catch (err) {
    console.error('Wallet connection error:', err);
    if (err.code === 4001) {
      showToast('❌', 'Rejected', 'You rejected the connection request.');
    } else {
      showToast('❌', 'Connection Failed', err.message || 'Unknown error. Try refreshing.');
    }
  }
}

function simulateDemoWallet() {
  walletAddress = 'tb1p' + Array.from({length:38}, function() { return '0123456789abcdef'[Math.floor(Math.random()*16)]; }).join('');
  walletBalance = parseFloat((Math.random() * 0.5 + 0.01).toFixed(4));
  finishConnect(walletAddress, walletBalance);
  showToast('ℹ️', 'Demo Mode', 'Install OPWallet Chrome extension for real testnet transactions');
}

function finishConnect(address, balance) {
  walletConnected = true;

  // Update UI
  const btn = document.getElementById('connectBtn');
  btn.textContent = address.slice(0, 8) + '...' + address.slice(-4);
  btn.classList.add('connected');

  document.getElementById('walletAddr').textContent = address;
  document.getElementById('walletBal').innerHTML = balance.toFixed(4) + ' <span style="font-size:16px;color:var(--muted)">tBTC</span>';
  document.getElementById('walletUSD').textContent = '≈ $' + (balance * 33700).toLocaleString(undefined, {maximumFractionDigits:2}) + ' USD';
  document.getElementById('walletOverlay').classList.add('open');

  // Show positions
  document.getElementById('noPositions').style.display = 'none';
  document.getElementById('positionsContent').style.display = 'block';

  showToast('✅', 'Wallet Connected', 'Connected to OP_NET Testnet (tBTC)');
}

function disconnectWallet() {
  walletConnected = false;
  walletAddress = null;
  walletBalance = 0;
  const btn = document.getElementById('connectBtn');
  btn.textContent = '🔐 Connect Wallet';
  btn.classList.remove('connected');
  document.getElementById('walletOverlay').classList.remove('open');
  document.getElementById('noPositions').style.display = 'block';
  document.getElementById('positionsContent').style.display = 'none';
  showToast('👋', 'Disconnected', 'Wallet disconnected');
}

async function requestFaucet() {
  showToast('🚰', 'Opening Faucet', 'Redirecting to faucet.opnet.org...');
  setTimeout(() => {
    window.open('https://faucet.opnet.org/', '_blank');
  }, 800);
}

// ============================================================
// TABS
// ============================================================
function showTab(tab) {
  document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
  document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.nav-links a').forEach(a => a.classList.remove('active'));
  document.getElementById('panel-' + tab).classList.add('active');
  document.getElementById('tab-' + tab).classList.add('active');
}

// ============================================================
// MODAL
// ============================================================
const modalIcons = { tBTC: { icon: '₿', cls: 'btc-icon' }, USDs: { icon: '$', cls: 'usds-icon' }, WBTC: { icon: 'Ƀ', cls: 'wbtc-icon' } };

function openModal(type, asset, apy) {
  if (!walletConnected) {
    showToast('⚠️', 'Wallet Required', 'Please connect your OP_NET wallet first');
    connectWallet();
    return;
  }
  currentModal = { type, asset, apy };
  const m = modalIcons[asset] || { icon: '?', cls: '' };
  document.getElementById('modalTitle').textContent = type === 'supply' ? 'Supply ' + asset : 'Borrow ' + asset;
  document.getElementById('modalIcon').textContent = m.icon;
  document.getElementById('modalIcon').className = 'asset-icon ' + m.cls;
  document.getElementById('modalAssetName').textContent = asset;
  document.getElementById('modalApy').textContent = apy + '%';
  document.getElementById('modalApy').style.color = type === 'supply' ? 'var(--green)' : 'var(--accent)';
  document.getElementById('modalBalance').textContent = asset === 'tBTC' ? walletBalance.toFixed(4) : '5000.00';
  document.getElementById('modalConfirmBtn').textContent = 'Confirm ' + (type.charAt(0).toUpperCase() + type.slice(1));
  document.getElementById('amountInput').value = '';
  document.getElementById('modalOverlay').classList.add('open');
}

function closeModal() {
  document.getElementById('modalOverlay').classList.remove('open');
  currentModal = null;
}

function closeModalIfBg(e) {
  if (e.target === document.getElementById('modalOverlay')) closeModal();
}

function setMax() {
  if (!currentModal) return;
  const maxVals = { tBTC: walletBalance.toFixed(4), USDs: '5000', WBTC: '0.1800' };
  document.getElementById('amountInput').value = maxVals[currentModal.asset] || '0';
}

async function confirmTx() {
  const amount = document.getElementById('amountInput').value;
  if (!amount || parseFloat(amount) <= 0) {
    showToast('⚠️', 'Invalid Amount', 'Please enter a valid amount');
    return;
  }
  const btn = document.getElementById('modalConfirmBtn');
  btn.textContent = 'Signing...';
  btn.disabled = true;

  try {
    if (typeof window.opnet !== 'undefined' && walletAddress && !walletAddress.startsWith('tb1p0')) {
      // Real transaction via OP_NET
      const txResult = await sendOpNetTransaction(currentModal.type, currentModal.asset, amount);
      handleTxSuccess(txResult);
    } else {
      // Demo simulation
      await simulateTransaction();
    }
  } catch (e) {
    showToast('❌', 'Transaction Failed', e.message);
  } finally {
    btn.textContent = 'Confirm ' + (currentModal?.type || '');
    btn.disabled = false;
    closeModal();
  }
}

async function sendOpNetTransaction(type, asset, amount) {
  const provider = window.opnet || window.bitcoin || window.unisat;
  if (!provider) throw new Error('OPWallet not found. Please install OPWallet extension.');

  const satAmount = Math.floor(parseFloat(amount) * 1e8);

  // Verifikasi saldo cukup sebelum kirim
  if (type === 'supply' && asset === 'tBTC') {
    if (satAmount > walletBalance * 1e8) {
      throw new Error('Insufficient tBTC balance. Get more from faucet.opnet.org');
    }
  }

  // Buat payload transaksi untuk OP_NET
  const txPayload = {
    to: LENDING_CONTRACT,
    value: satAmount.toString(),
    data: encodeCalldata(type, asset, satAmount),
    network: 'testnet',
    feeRate: 10  // sat/vbyte — bisa dinaikkan untuk konfirmasi lebih cepat
  };

  let txResult;

  const calldata = encodeCalldata(type, asset, satAmount);

  if (typeof provider.signAndBroadcastInteraction === 'function') {
    txResult = await provider.signAndBroadcastInteraction({
      to:          LENDING_CONTRACT,
      calldata:    calldata,
      satsPerByte: 10,
    });
  } else if (typeof provider.signInteraction === 'function') {
    const signed = await provider.signInteraction({
      to:          LENDING_CONTRACT,
      calldata:    calldata,
      satsPerByte: 10,
    });
    txResult = await provider.broadcast(signed);
  } else {
    const methods = Object.keys(provider).filter(k => typeof provider[k] === 'function');
    throw new Error('OPWallet method tidak ditemukan. Available: ' + methods.join(', '));
  }

  // Update saldo lokal setelah transaksi
  setTimeout(async function() {
    try {
      const newBal = await provider.getBalance();
      if (typeof newBal === 'object') {
        walletBalance = parseInt(newBal.confirmed || newBal.total || 0) / 1e8;
      } else {
        walletBalance = parseInt(newBal || 0) / 1e8;
      }
      document.getElementById('walletBal').innerHTML =
        walletBalance.toFixed(4) + ' <span style="font-size:16px;color:var(--muted)">tBTC</span>';
    } catch(e) { console.warn('Balance refresh failed:', e); }
  }, 3000);

  return txResult;
}

// Encode calldata untuk smart contract OP_NET
function encodeCalldata(type, asset, amountSats) {
  // Selector function OP_NET contract
  const selectors = {
    supply: 'a0712d68',
    borrow: 'd9d98ce4',
    repay:  'acb70815',
    withdraw: '2e1a7d4d'
  };
  const selector = selectors[type] || selectors.supply;
  const encodedAmount = amountSats.toString(16).padStart(64, '0');
  return '0x' + selector + encodedAmount;
}

// Build PSBT untuk fallback signing
async function buildPSBT(satAmount, type, asset) {
  // Placeholder — implementasi penuh butuh library bitcoin-psbt
  // Untuk production, gunakan @btc-vision/transaction
  return null;
}

async function simulateTransaction() {
  await new Promise(function(r) { setTimeout(r, 1800); });
  const fakeTxid = Array.from({length:64}, function() {
    return '0123456789abcdef'[Math.floor(Math.random()*16)];
  }).join('');
  handleTxSuccess({ txid: fakeTxid, status: 'confirmed' });
}

function handleTxSuccess(tx) {
  const txid = tx.txid || tx.hash || tx || ('demo_tx_' + Date.now());
  const txidStr = typeof txid === 'string' ? txid : JSON.stringify(txid);
  const label = currentModal
    ? (currentModal.type === 'supply' ? 'Supplied Successfully ✅' : 'Borrowed Successfully ✅')
    : 'Transaction Sent ✅';

  showToast('✅', label, 'TxID: ' + txidStr.slice(0, 20) + '...');

  // Buka explorer setelah 2 detik (hanya untuk transaksi nyata)
  if (txidStr.length === 64 && !txidStr.startsWith('demo')) {
    setTimeout(function() {
      if (confirm('Transaction sent! View on OP_NET Explorer?')) {
        window.open(EXPLORER_URL + txidStr, '_blank');
      }
    }, 2000);
  }
}

// ============================================================
// AI AGENT (MCP)
// ============================================================
const agentResponses = {
  'apy': () => `📊 **Current Market Rates on OP_NET**\n\n• **tBTC** Supply: 2.84% | Borrow: 5.61%\n• **USDs** Supply: 4.21% | Borrow: 7.85%\n• **WBTC** Supply: 3.15% | Borrow: 6.02%\n\nRates update dynamically based on utilization. USDs currently has the highest supply yield at 4.21%.`,
  'balance': () => walletConnected ? `💰 **Your Wallet Balance**\n\nAddress: \`${walletAddress?.slice(0,14)}...\`\n\n• **tBTC**: ${walletBalance.toFixed(4)} (≈ $${(walletBalance * 33700).toLocaleString()})\n• **USDs**: 5,000.00\n\nTo get more testnet tBTC, visit the [faucet](https://faucet.opnet.org/)` : `⚠️ Please connect your OP_NET wallet first to view your balance.`,
  'health': () => `🏥 **Health Factor Analysis**\n\nYour current health factor is **2.14** — this is considered **safe**.\n\n• Liquidation threshold: 1.0\n• Current collateral: 0.25 tBTC + 5,000 USDs\n• Current debt: 2,000 USDs\n\nYou can safely borrow up to 2,500 more USDs before entering risky territory.`,
  'strategy': () => `⚡ **Optimal tBTC Yield Strategy**\n\n**Option 1 - Simple Supply (Low Risk)**\n→ Supply tBTC at 2.84% APY\n→ Earn passive yield with no liquidation risk\n\n**Option 2 - Loop Strategy (Medium Risk)**\n→ Supply tBTC → Borrow USDs → Supply USDs\n→ Effective yield: ~3.2% net APY\n\n**Option 3 - Leverage (Higher Risk)**\n→ Supply tBTC as collateral, borrow more tBTC\n→ Up to 65% LTV — monitor health factor carefully\n\n⚠️ Always maintain health factor > 1.5`,
  'supply': async function(msg) {
    var match = msg.match(/(\d+\.?\d*)\s*(tbtc|usds|wbtc)/i);
    if (match) {
      var amt = match[1];
      var asset = match[2].toUpperCase();
      var apyVal = asset === 'tBTC' ? '2.84' : asset === 'USDs' ? '4.21' : '3.15';
      var walletStatus = walletConnected ? '🟢 Wallet connected' : '⚠️ Please connect wallet first';
      return '✅ **Supply Order Prepared**\n\nAsset: **' + asset + '**\nAmount: **' + amt + '**\nExpected APY: **' + apyVal + '%**\n\nTo execute this transaction, click the **Supply** button in the Markets tab. ' + walletStatus;
    }
    return 'To supply assets, please specify the amount and asset. Example: "Supply 0.1 tBTC" or use the Markets tab.';
  },
  'default': (msg) => `I received your message: "${msg}"\n\nI'm connected to OP_NET via MCP at \`ai.opnet.org/mcp\`. I can help with:\n• Market rates and analytics\n• Position management\n• Transaction execution\n• Risk assessment\n\nWhat would you like to know?`
};

async function sendChat() {
  const input = document.getElementById('chatInput');
  const msg = input.value.trim();
  if (!msg || isAgentThinking) return;
  input.value = '';
  appendMsg('user', msg);
  isAgentThinking = true;
  showTyping();

  try {
    const response = await callMCPAgent(msg);
    removeTyping();
    appendMsg('bot', response);
  } catch(e) {
    removeTyping();
    appendMsg('bot', '⚠️ Unable to connect to ai.opnet.org/mcp — using local fallback.\n\n' + getLocalResponse(msg));
  }
  isAgentThinking = false;
}

async function callMCPAgent(userMessage) {
  // Call the Anthropic API as the AI backbone, with OP_NET MCP context
  const systemPrompt = `You are BitLend AI Agent, an intelligent DeFi assistant for the BitLend lending protocol built on OP_NET — a Bitcoin Layer 1 smart contract platform.

You are connected to the OP_NET MCP server at https://ai.opnet.org/mcp.

Current protocol state:
- tBTC: Supply APY 2.84%, Borrow APY 5.61%, Utilization 43%
- USDs: Supply APY 4.21%, Borrow APY 7.85%, Utilization 67%
- WBTC: Supply APY 3.15%, Borrow APY 6.02%, Utilization 39%
- Total TVL: $4.21M
- Network: OP_NET Testnet (tBTC)
- Wallet connected: ${walletConnected}
${walletConnected ? `- User wallet: ${walletAddress}` : ''}

Respond concisely and helpfully. For transaction requests, guide users to use the Supply/Borrow buttons. Format numbers clearly. Use emojis sparingly for clarity.`;

  const response = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      model: 'claude-sonnet-4-20250514',
      max_tokens: 1000,
      system: systemPrompt,
      messages: [{ role: 'user', content: userMessage }]
    })
  });

  if (!response.ok) throw new Error('API error: ' + response.status);
  const data = await response.json();
  return data.content?.[0]?.text || 'No response from agent.';
}

function getLocalResponse(msg) {
  const lower = msg.toLowerCase();
  if (lower.includes('apy') || lower.includes('rate')) return agentResponses.apy();
  if (lower.includes('balance') || lower.includes('wallet')) return agentResponses.balance();
  if (lower.includes('health')) return agentResponses.health();
  if (lower.includes('strategy') || lower.includes('yield')) return agentResponses.strategy();
  return agentResponses.default(msg);
}

function sendQuick(msg) {
  document.getElementById('chatInput').value = msg;
  sendChat();
}

function appendMsg(type, text) {
  const container = document.getElementById('chatMessages');
  const isBot = type === 'bot';
  const now = new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
  const div = document.createElement('div');
  div.className = 'msg ' + (isBot ? 'bot' : 'user');
  div.innerHTML = `
    <div class="msg-avatar ${isBot ? 'bot' : 'user-av'}">${isBot ? '🤖' : '👤'}</div>
    <div>
      <div class="msg-bubble">${text.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>').replace(/`(.*?)`/g, '<code>$1</code>').replace(/\n/g, '<br>')}</div>
      <div class="msg-time">${now}</div>
    </div>
  `;
  container.appendChild(div);
  container.scrollTop = container.scrollHeight;
}

function showTyping() {
  const container = document.getElementById('chatMessages');
  const div = document.createElement('div');
  div.className = 'msg bot';
  div.id = 'typingMsg';
  div.innerHTML = `
    <div class="msg-avatar bot">🤖</div>
    <div>
      <div class="msg-bubble">
        <div class="typing-indicator">
          <div class="typing-dot"></div>
          <div class="typing-dot"></div>
          <div class="typing-dot"></div>
        </div>
      </div>
    </div>`;
  container.appendChild(div);
  container.scrollTop = container.scrollHeight;
}

function removeTyping() {
  const el = document.getElementById('typingMsg');
  if (el) el.remove();
}

// ============================================================
// TOAST
// ============================================================
function showToast(icon, title, msg) {
  const t = document.getElementById('toast');
  document.getElementById('toastIcon').textContent = icon;
  document.getElementById('toastTitle').textContent = title;
  document.getElementById('toastMsg').textContent = msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 4000);
}

// ============================================================
// LIVE STATS ANIMATION
// ============================================================
function animateStats() {
  const tvlEl = document.getElementById('tvl');
  const borrowedEl = document.getElementById('borrowed');
  let base = 4210000, bBase = 1870000;
  setInterval(() => {
    base += Math.floor((Math.random() - 0.45) * 5000);
    bBase += Math.floor((Math.random() - 0.45) * 3000);
    tvlEl.textContent = '$' + (base / 1e6).toFixed(2) + 'M';
    borrowedEl.textContent = '$' + (bBase / 1e6).toFixed(2) + 'M';
  }, 4000);
}

animateStats();

// Close wallet dropdown when clicking outside
document.addEventListener('click', (e) => {
  const overlay = document.getElementById('walletOverlay');
  const btn = document.getElementById('connectBtn');
  if (overlay.classList.contains('open') && !overlay.contains(e.target) && e.target !== btn) {
    overlay.classList.remove('open');
  }
});
</script>
</body>
</html>